# - output infoText -
output$infoText <- renderText({
        rv$infoText
})

output$infoTextExtra3 <- renderText({
        rv$infoText
})

output$infoTextExtra4 <- renderText({
        rv$infoText
})

output$infoTextExtra5 <- renderText({
        rv$infoText
})
# --