# - output infoText -
output$infoText <- renderText({
        rv$infoText
})

output$infoTextExtra1 <- renderText({
        rv$infoText
})

output$infoTextExtra2 <- renderText({
        rv$infoText
})
# --